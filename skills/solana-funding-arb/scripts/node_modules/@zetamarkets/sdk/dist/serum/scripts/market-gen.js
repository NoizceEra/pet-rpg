"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const assets = __importStar(require("../../assets"));
const utils = __importStar(require("../../utils"));
const exchange_1 = require("../../exchange");
const network_1 = require("../../network");
const constants = __importStar(require("../../constants"));
const web3_js_1 = require("@solana/web3.js");
const anchor = __importStar(require("@zetamarkets/anchor"));
const fs = __importStar(require("fs"));
const market_gen_utils_1 = require("./market-gen-utils");
const mainnetUrl = "";
const devnetUrl = "";
const main = async () => {
    let networks = [network_1.Network.MAINNET];
    let assetList = assets.allAssets();
    for (var network of networks) {
        let marketStore = {};
        let connectionUrl = network == network_1.Network.DEVNET ? devnetUrl : mainnetUrl;
        const LOAD_CONFIG = {
            network: network,
            connection: new web3_js_1.Connection(connectionUrl, utils.defaultCommitment()),
            opts: utils.defaultCommitment(),
            throttleMs: 0,
            loadFromStore: false,
            TIFBufferSeconds: 0,
        };
        // Note: If exchange.load() is failing you may need to use the old code before we sped up loading
        // Commit: https://github.com/zetamarkets/sdk/commit/c6ff9376a5cc9255b2dcfffaa330d3bac1446954#diff-c15e612fadecac34c3bc31bc36799f0f4be12ee1703b1a1753a89ae644f55305
        await exchange_1.exchange.load(LOAD_CONFIG);
        for (var asset of assetList) {
            populateMarketStore(marketStore, asset, constants.PERP_INDEX);
        }
        await exchange_1.exchange.close();
        const FILENAME = `MARKET-STORE.${network}.json`;
        fs.writeFile(FILENAME, JSON.stringify(marketStore), (_) => { });
    }
};
main().catch(console.error.bind(console));
function populateMarketStore(marketStore, asset, index) {
    if (!(asset in marketStore)) {
        marketStore[asset] = {};
    }
    if (index in marketStore[asset]) {
        return;
    }
    marketStore[asset][index] = {};
    let store = marketStore[asset][index];
    let subExchange = exchange_1.exchange.getSubExchange(asset);
    if (index != constants.PERP_INDEX) {
        throw Error("non-perps not supported");
    }
    let decodedSerumMarket = subExchange.markets.market.serumMarket.decoded;
    for (const k in decodedSerumMarket) {
        let value = decodedSerumMarket[k];
        switch ((0, market_gen_utils_1.returnDecodedType)(k)) {
            case market_gen_utils_1.DecodeType.AccountFlags: {
                store[k] = value;
                break;
            }
            case market_gen_utils_1.DecodeType.BN: {
                store[k] = new anchor.BN(value).toString();
                break;
            }
            case market_gen_utils_1.DecodeType.PublicKey: {
                store[k] = value.toString();
                break;
            }
        }
    }
}
